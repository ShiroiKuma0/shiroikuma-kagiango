<?xml version="1.0" encoding="utf-8"?>
<!-- 白い熊 鍵暗号 UI page SUB-heading (one level under a section, kxkb house style): a medium-bold accent
     heading with a word-width underline. No full-width spacer — that line marks top-level groups only. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="2dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/subgroup_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="17sp"
            android:textStyle="bold"
            android:text="Subgroup" />

        <View
            android:id="@+id/subgroup_divider"
            android:layout_width="match_parent"
            android:layout_height="1.5dp"
            android:layout_marginTop="2dp" />
    </LinearLayout>
</LinearLayout>
